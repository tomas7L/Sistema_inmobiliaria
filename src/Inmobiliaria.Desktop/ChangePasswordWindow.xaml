<Window x:Class="Inmobiliaria.Desktop.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Inmobiliaria.Desktop"
        mc:Ignorable="d"
        Title="Cambiar contraseña"
        Height="360" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding DisplayName, StringFormat='Usuario: {0}'}"
                   Margin="0,0,0,12" />

        <TextBlock Grid.Row="1" Text="Contraseña actual" Margin="0,0,0,4" />
        <PasswordBox x:Name="CurrentPasswordInput"
                     Grid.Row="2"
                     PasswordChanged="CurrentPasswordInput_PasswordChanged"
                     Margin="0,0,0,12" />

        <TextBlock Grid.Row="3" Text="Contraseña nueva" Margin="0,0,0,4" />
        <PasswordBox x:Name="NewPasswordInput"
                     Grid.Row="4"
                     PasswordChanged="NewPasswordInput_PasswordChanged"
                     Margin="0,0,0,12" />

        <TextBlock Grid.Row="5" Text="Repetir contraseña nueva" Margin="0,0,0,4" />
        <PasswordBox x:Name="ConfirmNewPasswordInput"
                     Grid.Row="6"
                     PasswordChanged="ConfirmNewPasswordInput_PasswordChanged"
                     Margin="0,0,0,12" />

        <TextBlock Grid.Row="7"
                   Text="{Binding ErrorMessage}"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar"
                    Command="{Binding CancelCommand}"
                    Visibility="{Binding IsForced, Converter={StaticResource BoolToVisibility}}"
                    Margin="0,0,8,0"
                    MinWidth="90" />
            <Button Content="Cambiar contraseña"
                    Command="{Binding ChangeCommand}"
                    IsDefault="True"
                    MinWidth="140" />
        </StackPanel>
    </Grid>
</Window>
