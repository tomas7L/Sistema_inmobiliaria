<Window x:Class="Inmobiliaria.Desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Inmobiliaria.Desktop"
        mc:Ignorable="d"
        Title="Iniciar sesión"
        Height="260" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Usuario" Margin="0,0,0,4" />
        <TextBox Grid.Row="1"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12" />

        <TextBlock Grid.Row="2" Text="Contraseña" Margin="0,0,0,4" />
        <PasswordBox x:Name="PasswordInput"
                     Grid.Row="3"
                     PasswordChanged="PasswordInput_PasswordChanged"
                     Margin="0,0,0,12" />

        <TextBlock Grid.Row="4"
                   Text="{Binding ErrorMessage}"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ingresar"
                    Command="{Binding LoginCommand}"
                    IsDefault="True"
                    MinWidth="90" />
        </StackPanel>
    </Grid>
</Window>
